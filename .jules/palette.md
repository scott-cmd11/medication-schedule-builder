# Palette's Journal

## 2024-05-22 - Delete Confirmation Pattern
**Learning:** Destructive actions in list views (like removing medications) require confirmation to prevent accidental data loss, especially on mobile where touch targets can be small.
**Action:** Implemented an inline confirmation pattern (replacing the action button with 'Confirm'/'Cancel') rather than a modal, to keep context and reduce friction.
